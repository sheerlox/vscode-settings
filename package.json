{
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.1.0",
    "prettier": "3.1.0"
  },
  "lint-staged": {
    "*.{json,md}": "prettier --write"
  },
  "scripts": {
    "prepare": "husky install"
  }
}
